# AgentCommit — Claude Code Instructions

## Commit Messages

- **Never** include `Co-Authored-By: Claude` (or any variant) in commit messages.
- Write commit messages in conventional commit format: `type(scope): description`
- Keep the subject line ≤72 characters, imperative mood, no trailing period.
